export class LiveNews {

    id: string;
    content: string;
    grade: string;
    // tslint:disable-next-line:variable-name
    created_at: string;
    // tslint:disable-next-line:variable-name
    up_counts: string;
    // tslint:disable-next-line:variable-name
    down_counts: string;
}
