import { LiveNewsCommentLike } from './live-news-comment-like';

describe('LiveNewsCommentLike', () => {
  it('should create an instance', () => {
    expect(new LiveNewsCommentLike()).toBeTruthy();
  });
});
