import { SochainBtcTransaction } from './sochain-btc-transaction';

describe('SochainBtcTransaction', () => {
  it('should create an instance', () => {
    expect(new SochainBtcTransaction()).toBeTruthy();
  });
});
