import { SochainLtcTransaction } from './sochain-ltc-transaction';

describe('SochainLtcTransaction', () => {
  it('should create an instance', () => {
    expect(new SochainLtcTransaction()).toBeTruthy();
  });
});
