import { ListingLatestQuoteUSD } from './listing-latest-quote-usd';

describe('ListingLatestQuoteUSD', () => {
  it('should create an instance', () => {
    expect(new ListingLatestQuoteUSD()).toBeTruthy();
  });
});
