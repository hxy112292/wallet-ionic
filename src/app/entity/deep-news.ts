import {DeepNewsExtra} from './deep-news-extra';

export class DeepNews {

    id: string;
    title: string;
    // tslint:disable-next-line:variable-name
    short_title: string;
    extra: DeepNewsExtra;
}
