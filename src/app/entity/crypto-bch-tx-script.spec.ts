import { CryptoBchTxScript } from './crypto-bch-tx-script';

describe('CryptoBchTxScript', () => {
  it('should create an instance', () => {
    expect(new CryptoBchTxScript()).toBeTruthy();
  });
});
