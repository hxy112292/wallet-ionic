import { RippleTransactionOutcome } from './ripple-transaction-outcome';

describe('RippleTransactionOutcome', () => {
  it('should create an instance', () => {
    expect(new RippleTransactionOutcome()).toBeTruthy();
  });
});
