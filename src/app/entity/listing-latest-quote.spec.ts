import { ListingLatestQuote } from './listing-latest-quote';

describe('ListingLatestQuote', () => {
  it('should create an instance', () => {
    expect(new ListingLatestQuote()).toBeTruthy();
  });
});
