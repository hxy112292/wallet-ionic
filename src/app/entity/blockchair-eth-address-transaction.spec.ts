import { BlockchairEthAddressTransaction } from './blockchair-eth-address-transaction';

describe('BlockchairEthAddressTransaction', () => {
  it('should create an instance', () => {
    expect(new BlockchairEthAddressTransaction()).toBeTruthy();
  });
});
