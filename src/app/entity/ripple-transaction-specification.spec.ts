import { RippleTransactionSpecification } from './ripple-transaction-specification';

describe('RippleTransactionSpecification', () => {
  it('should create an instance', () => {
    expect(new RippleTransactionSpecification()).toBeTruthy();
  });
});
